:80 {
    # 1. API requests go to Node.js
    handle /api/* {
        reverse_proxy backend:5000
    }

    # 2. Everything else goes to the Frontend Nginx container
    handle {
        reverse_proxy frontend:80
    }
}